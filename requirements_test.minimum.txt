# Pins for the minimum-supported Home Assistant CI lane.
# Compile a reproducible lockfile after changing this file (target Linux CI):
#   uv pip compile requirements_test.txt requirements_test.minimum.txt \
#     -o requirements_test.minimum.lock.txt \
#     --python-version 3.14.2 --python-platform linux --generate-hashes
# Keep homeassistant aligned with hacs.json "homeassistant" and pair it with
# the matching pytest-homeassistant-custom-component release.
-r requirements_test.txt
homeassistant==2026.7.0
pytest-homeassistant-custom-component==0.13.344
