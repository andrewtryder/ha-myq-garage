# No runtime dependencies beyond Home Assistant core.
# Integration-owned client/models ship under custom_components/myq_garage/
# and are installed with the HACS custom component (manifest requirements: []).
