# Unpinned test/tooling dependency inputs.
# Compile a reproducible lockfile after changing this file (target Linux CI):
#   uv pip compile requirements_test.txt -o requirements_test.lock.txt \
#     --python-version 3.14.2 --python-platform linux --generate-hashes
# CI pull requests install from requirements_test.lock.txt; the scheduled
# canary job installs from this file to catch upcoming ecosystem breakages.
pytest
pytest-homeassistant-custom-component
pytest-cov
pytest-asyncio
aresponses
ruff
pre-commit
mypy
